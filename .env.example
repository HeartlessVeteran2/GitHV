# Example Environment Configuration
# Copy this file to .env and update with your values

# Application Environment
NODE_ENV=development
PORT=5000

# Database Configuration
# For development, you can use a local PostgreSQL instance:
# DATABASE_URL=postgresql://username:password@localhost:5432/githv
# For production, use a proper database URL with SSL:
DATABASE_URL=postgresql://username:password@your-db-host:5432/githv?ssl=true

# Security
# Generate a secure random string for production (at least 32 characters)
SESSION_SECRET=your-super-secure-session-secret-change-this-in-production

# GitHub OAuth Configuration
# Create a GitHub OAuth App at https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# AI Integration (Optional)
# Get your Gemini API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# Redis Configuration (Optional - for session storage)
# If not provided, sessions will use memory store (not recommended for production)
REDIS_URL=redis://localhost:6379

# Logging
LOG_LEVEL=info

# CORS Configuration (Optional)
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,https://yourdomain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Development-only variables
# These are ignored in production

# Production-only variables
# SSL_KEY=/path/to/ssl/key.pem
# SSL_CERT=/path/to/ssl/cert.pem